---
phase: 04-polish-i-preparacio-vps
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - web/app.py
  - web/requirements.txt
autonomous: true
requirements:
  - NF4

must_haves:
  truths:
    - "Un fitxer .docx vàlid és acceptat i processat correctament"
    - "Un fitxer amb extensió .docx però contingut no-DOCX (p. ex. un .txt renombrat) és rebutjat amb un missatge d'error clar"
    - "Un fitxer de més de 20 MB és rebutjat amb el missatge d'error existent"
    - "El codi de app.py fa servir secure_filename i prefix UUID (ja existent; verificar que és al lloc correcte)"
    - "requirements.txt inclou puremagic i gunicorn"
  artifacts:
    - path: "web/app.py"
      provides: "Funció validar_mime() i crida just després de fitxer.save(), import puremagic, ALLOWED_MIME set"
    - path: "web/requirements.txt"
      provides: "Línies puremagic>=1.28 i gunicorn>=23.0"
  key_links:
    - from: "web/app.py POST /corregir"
      to: "puremagic.magic_file(input_path)"
      via: "validar_mime() cridat just després de fitxer.save()"
      pattern: "validar_mime|puremagic"
---

<objective>
Afegir validació MIME del fitxer pujat (servidora, a partir dels bytes reals) i actualitzar requirements.txt amb les noves dependències de producció.

Purpose: La validació actual comprova només l'extensió del fitxer, que qualsevol pot falsificar. La validació MIME llegeix els bytes màgics del fitxer real per confirmar que és un ZIP/DOCX vàlid. Aquesta és la capa de seguretat que faltava (NF4). El path traversal ja està cobert per secure_filename + UUID (verificar-ho).
Output: app.py amb validació MIME post-save; requirements.txt amb puremagic i gunicorn.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@web/app.py
@web/requirements.txt
</context>

<interfaces>
<!-- Estat actual de app.py rellevant per a aquest pla -->

Ruta POST /corregir (app.py, línia ~54):
- rep fitxer = request.files.get('fitxer')
- comprova extensió: ext = Path(fitxer.filename).suffix.lower()
- desa: fitxer.save(input_path)  ← INSERIR validació MIME AQUÍ (just després)
- crida InDretCorrector(...)

Patró de nom: input_path = UPLOAD_DIR / f"{sid}_input_{nom_segur}"
→ secure_filename + UUID ja present: path traversal mitigat.

MAX_CONTENT_LENGTH = 20 * 1024 * 1024 ja configurat.
@app.errorhandler(RequestEntityTooLarge) ja present.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Actualitzar requirements.txt</name>
  <files>web/requirements.txt</files>
  <action>
Substituir el contingut de web/requirements.txt per:

```
Flask>=3.0
Werkzeug>=3.0
puremagic>=1.28
gunicorn>=23.0
```

Ordre: Flask, Werkzeug, puremagic, gunicorn. Una dependència per línia, sense comentaris.
  </action>
  <verify>
    <automated>grep -c "puremagic" "/Volumes/1Tera/Local Sites/indret-prod/app/public/wp-content/themes/indret/web/requirements.txt" && grep -c "gunicorn" "/Volumes/1Tera/Local Sites/indret-prod/app/public/wp-content/themes/indret/web/requirements.txt"</automated>
  </verify>
  <done>requirements.txt conté les quatre dependències: Flask, Werkzeug, puremagic, gunicorn</done>
</task>

<task type="auto">
  <name>Task 2: Afegir validació MIME a app.py</name>
  <files>web/app.py</files>
  <action>
Fer dos canvis a web/app.py:

**Canvi 1 — Imports al capdamunt del fitxer:**
Afegir `import puremagic` just DESPRÉS de la línia `import uuid` (o on estiguin els imports de la stdlib). La línia nova:
```python
import puremagic
```

**Canvi 2 — Constant ALLOWED_MIME:**
Afegir just ABANS de la línia `ALLOWED_EXT = {'.docx'}` (o just DESPRÉS, a la mateixa secció de constants):
```python
ALLOWED_MIME = {
    'application/vnd.openxmlformats-officedocument.wordprocessingml.document',
    'application/zip',  # DOCX és un contenidor ZIP; puremagic pot retornar aquest
}
```

**Canvi 3 — Funció helper:**
Afegir una funció `validar_mime` just ABANS de la funció `netejar_uploads`:
```python
def validar_mime(path: Path) -> bool:
    """Retorna True si el fitxer sembla un DOCX segons els bytes màgics."""
    try:
        matches = puremagic.magic_file(str(path))
        if not matches:
            return False
        return matches[0].mime_type in ALLOWED_MIME
    except puremagic.MagicException:
        return False
```

**Canvi 4 — Crida a validar_mime a la ruta /corregir:**
A la funció `corregir()`, just DESPRÉS de la línia `fitxer.save(input_path)` i ABANS de les línies que llegeixen `edicio`, `doi`, etc., inserir:
```python
    if not validar_mime(input_path):
        input_path.unlink(missing_ok=True)
        flash('El fitxer no és un document .docx vàlid.')
        return redirect(url_for('index'))
```

IMPORTANT: Acceptar tant el MIME DOCX complet com `application/zip` (DOCX és un ZIP; puremagic retorna sovint `application/zip` per a fitxers DOCX vàlids). Rebutjar qualsevol altra cosa.

No modificar cap altra lògica existent. No canviar la validació d'extensió (mantenir-la com a primera línia de defensa).
  </action>
  <verify>
    <automated>cd "/Volumes/1Tera/Local Sites/indret-prod/app/public/wp-content/themes/indret/web" && python3 -c "
import ast, sys
with open('app.py') as f:
    src = f.read()
tree = ast.parse(src)
names = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)]
assert 'validar_mime' in names, 'validar_mime function missing'
assert 'puremagic' in src, 'puremagic import missing'
assert 'ALLOWED_MIME' in src, 'ALLOWED_MIME constant missing'
print('OK: validar_mime, puremagic, ALLOWED_MIME all present')
"</automated>
  </verify>
  <done>app.py conté la funció validar_mime, l'import puremagic, la constant ALLOWED_MIME, i la crida a validar_mime just després de fitxer.save()</done>
</task>

</tasks>

<verification>
Instal·lar puremagic i provar manualment:
```bash
cd web && pip install puremagic
python app.py
```
- Pujar un .docx real → acceptat i processat
- Crear un fitxer de text buit, renombrar-lo a `fals.docx`, pujar-lo → missatge d'error "El fitxer no és un document .docx vàlid."
- Verificar que secure_filename + UUID prefix estan presents a la línia input_path (ja existent, no cal modificar)
</verification>

<success_criteria>
- Fitxers DOCX vàlids superen la validació MIME
- Fitxers no-DOCX (ZIP d'un altre tipus, text pla, etc.) renombrats a .docx són rebutjats
- requirements.txt reflecteix totes les dependències de producció
</success_criteria>

<output>
Després de completar, crear `.planning/phases/04-polish-i-preparacio-vps/04-02-SUMMARY.md`
</output>
